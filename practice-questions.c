#include <stdio.h>
int main(){

int length, breadth;
     printf("today we're going to calculate the area of a rectanlge");
     printf("Enter the value of length \n");
     scanf("%d", &length);
     printf("enter the value of breadth \n");
     scanf("%d", &breadth);
     printf("the area of the rectangle is equal to %d", length * breadth);
     return 0;
}